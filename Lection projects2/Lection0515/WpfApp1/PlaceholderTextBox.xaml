<UserControl x:Class="WpfApp1.PlaceholderTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid Background="White">
        <TextBlock x:Name="HintTextBlock" Foreground="Gray" Margin="3, 2" Text="Поиск ... "/>
        <TextBox x:Name="ContentTextBox" Background="Transparent" TextChanged="ContentTextBox_TextChanged" Text="{Binding Text}"/>
    </Grid>
</UserControl>
